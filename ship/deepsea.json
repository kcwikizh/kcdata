---
---
[
  {% assign ships = site.ship | where_exp: "item", "item.data.id >= 1500" | sort: "data.id" %}{% for ship in ships %}{{ ship.data | jsonify }}{% unless forloop.last %},
  {% endunless %}{% endfor %}
]
